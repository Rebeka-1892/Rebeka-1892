package main;

import objet.*;
import Fonction.*;
import mot.*;

import java.io.*;

public class Main {

    public static void main(String[] args) throws Exception{

        // String[] Str = new String[2];
        // Str[0] = "Id";
        // Str[1] = "Prenom";

        // String[] condition = new String[2];
        // condition[0] = "Prenom";
        // condition[1]="Age";

        // String[] ValCond = new String[2];
        // ValCond[0] = "Harena";
        // ValCond[1]="19";        

        // String[] Ing={"Ing"};
        // String[] Rec={"Rec"};

        // File Ingredients = new File("Ingredients.txt");
        // File ColIngredients = new File("NomCol.txt");
        // File ColRecettes = new File("Col2.txt");
        // File Recettes=new File("Recette.txt");   

        // Reading read= new Reading();
        // Object[][] obj=read.ReadFichier(Ingredients);
        // String[] column=read.ColumName(ColIngredients);
        // Object[][] etu=read.ReadFichier(Recettes);
        // String[] colum2=read.ColumName(ColRecettes);

        // File NomEtu= new File("NomEtu.txt");
        // File NomEtuCol= new File("NomEtuCol.txt");
        // File PrenomEtu= new File("PrenomEtu.txt");
        // File PrenomEtuCol= new File("PrenomEtuCol.txt");

        // Object[][] NomObj = read.ReadFichier(NomEtu);
        // String[] NomColObj = read.ColumName(NomEtuCol);
        // Object[][] PrenomObj = read.ReadFichier(PrenomEtu);
        // String[] PrenomColObj = read.ColumName(PrenomEtuCol);
        // String FCommune="Id";
       
        // Fonction Fonction = new Fonction();
        // Fonction.Projection(obj, column, Ing);
        // Fonction.Selection(obj, column, Str,condition, ValCond);
        // Fonction.Cartesien(obj, etu);
        // Fonction.Difference(obj, column, etu, column);
        // Fonction.Division(obj, column, Ing, etu, colum2, Rec);
        // Fonction.Jointure(NomObj, NomColObj, PrenomObj, PrenomColObj, FCommune);
        // Fonction.Union(NomObj, NomColObj, PrenomObj, PrenomColObj);
        // Fonction.Intersection(NomObj, NomColObj, PrenomObj, PrenomColObj);
        // Fonction.CreateTable("Mody", "Par Mr Rojo");
        // Fonction.Insert("Mody", "Par Mr tahiana");

        // Vocabulaire Vocabulaire = new Vocabulaire();
        // String txt = "Select * from Employe";
        // // Vocabulaire.Projection(txt, Employe);
        // txt = "Select * from Employe where Prenom=Rebeka and Age=17";
        // // Vocabulaire.Selection(txt, Employe);
        // txt="Select difference between Employe and Etudiant";
        // // Vocabulaire.Difference(txt, Employe, Etudiant);
    }
}